
BASIC_AUTH_PASSWORD=555
POSTGRES_PASSWORD=555

# The current Compose worker runs the deterministic fake executor only.
# TrueNAS credentials are intentionally not used by this profile yet.
PUBLISH_EXECUTOR_MODE=fake
WORKER_POLL_INTERVAL_SECONDS=1
DRAMATIQ_WORKER_THREADS=2
